{
  "name": "react-app",
  "version": "1.0.0",
  "description": "React app assignment 1 for csc 307",
  "main": "index.js",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd packages/react-frontend && npm start"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify-zlib": "^0.2.0"
  }
}
