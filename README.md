React app assignment 1 for csc 307

to test the delete method:
i suggest having two terminals open, one running the service (local host), and one to run this command:
curl -X DELETE http://localhost:8000/users/ppp222